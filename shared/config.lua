Config = {}

Config.frequency = 500 -- Frequency in milliseconds to check for death and sync (lower values increase responsiveness but may impact performance) - 500 is tested, lower is not tested (change at own risk)